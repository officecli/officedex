/**
 * Wave 0 seam: Home's feature-video shelf, not built yet.
 *
 * The prototype puts a row of short films here — one per document type plus
 * one for projects — with a carousel and a modal player. There is no footage
 * in the repository, so the shelf ships as placeholder cards whose click says
 * the videos are on their way rather than opening an empty player.
 *
 * Rendering nothing until then: an empty section with a heading reads as a
 * loading failure.
 */
export function Highlights() {
  return null;
}
