// UI limits that more than one component used to declare for itself.

/** Up to this many options a question is answered by number keys. */
export const MAX_NUMBERED_OPTIONS = 4;

/** How many agent runs the runtime panels ask the bridge for at once. */
export const AGENT_RUN_FETCH_LIMIT = 500;
