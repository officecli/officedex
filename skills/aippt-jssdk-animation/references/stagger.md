# 交错出现

适用于并列卡片、列表与时间轴内容。使用静态来源确定的阅读顺序；每个内容对象或原生组一个 FadeIn，durationMs 450，delayMs = 300 + index * 500，start 为 withPrevious。首版一页最多 16 个事件，按项数增加页时长，末项完整出现后保留至少 1000ms。不要按数组随机顺序安排，不把层级关系误当时间关系。证据见 registry 对应条目。
