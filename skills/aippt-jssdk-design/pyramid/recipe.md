# 金字塔配方（candidate_case）

适合 3–6 个递进层级或总分结构。用面积、位置或颜色表达层级，顶部是结论或总项，底部是基础项；每层文字保持同一阅读方向并留出足够内边距。

反例：仅有并列关系或需要精确数值比例的图表。层级含义不明确时回退到列表。

## 已核验变体

当前可执行变体见 variants.json；依据实际预览和源码确认关系，不按目录名推断。加载选择结果的 evidence 后，保留其分叉或分层构图；标题和正文容量是需要重新测量的衍生假设。
